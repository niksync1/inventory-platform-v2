# Feature modules

Each business capability owns its UI, hooks, API adapter, and tests. Shared business
rules remain in `src/core`, while reusable presentation code belongs in `src/shared`.

Planned modules: `auth`, `products`, `stock-movements`, `offline-sync`, `reports`,
`buyers`, and `admin`.
